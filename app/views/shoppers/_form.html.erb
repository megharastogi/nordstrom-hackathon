<%= form_for(shopper) do |f| %>
  <% if shopper.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shopper.errors.count, "error") %> prohibited this shopper from being saved:</h2>

      <ul>
      <% shopper.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :loyalty %>
    <%= f.text_field :loyalty %>
  </div>

  <div class="field">
    <%= f.label :department %>
    <%= f.text_field :department %>
  </div>

  <div class="field">
    <%= f.label :nord_shopper_id %>
    <%= f.text_field :nord_shopper_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
